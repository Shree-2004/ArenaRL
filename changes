remaining implementation of • SARSA – Conservative on-policy learning for grid-based games

give a document encompassing how the training of the agents is going on in the games
share a document which shows how the agents are being trained the client needs to understand if the agents are being actually trained or is it just a mock data

check out the pong game winner schema even tho player 2 wins it counts as player 1 is only winning

how are 2 different agents in some other game able t play some other game
for example if one agent trained for connect 4 and other is trained for tic tac toe but if in connect 4 the tic tac toe agent is used then also its able to play the game how is that possible check and fix that

training episodes even tho they were set to different numbers 

export as pdf and excel not working in the stats page

we need to add these games:- Dodge, 2048,Breakout, Grid Treasure Hunt, Balloon Pop.